/* eslint-disable react/prop-types */


const SingleRajnoitikDol = ({ a }) => {
    const { id, name, website_url, logo, sign_name, registration_date, registration_no } = a;
    
    return (
        <div>
            
            <p>ok</p>
        </div>
    );
};

export default SingleRajnoitikDol;